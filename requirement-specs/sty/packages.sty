\ProvidesPackage{sty/packages}

% Configure font
\usepackage{helvet}
\renewcommand{\sfdefault}{phv}
\renewcommand{\familydefault}{\sfdefault}

% Links in table of contents
\usepackage[hidelinks]{hyperref}

% General page dimensions
\usepackage[letterpaper, top=1in, bottom=1in, left=1in, right=1in]{geometry}

% Fancy page options
\usepackage{fancyhdr}

% Fix \headheight warning (see https://tex.stackexchange.com/a/327298)
\setlength{\headheight}{15pt}

\usepackage[pdftex]{graphicx}
\usepackage[utf8]{inputenc}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{wasysym}

% Columns if needed
\usepackage{multicol}

% \begin{comment} environment
\usepackage{comment}

% Colors!
\usepackage{color}

% More options for tables
\usepackage{array}

% Code blocks and syntax highlighting
\usepackage{minted}

% Highlighting, \hl{texthere}
\usepackage{soul}

% Subfigures, subtables, etc
\usepackage{subcaption}

% Reference last page number
\usepackage{lastpage}

% Bibliography (see https://en.wikibooks.org/wiki/LaTeX/Bibliographies_with_biblatex_and_biber)
\usepackage[
    backend=biber, % biber or bibtex
    style=ieee,
    citestyle=numeric-comp
]{biblatex}
\bibliography{references}

% Automate definitions, acronyms, and abbreviations
% `nonumberlist` is used here to remove the page number of references
% Use `numberedsection` to add subsections for definitions and acronyms
\usepackage[acronym, nonumberlist, section=subsection]{glossaries}
\makenoidxglossaries
% \makeglossaries didn't work, see link for details (search "noidx")
% http://tug.ctan.org/tex-archive/macros/latex/contrib/glossaries/glossariesbegin.html

% For special positioning of the WSU-V banner
% \usepackage{tikzpagenodes}

% Drawing and graphs
\usepackage{tikz}
\usetikzlibrary{automata, positioning, arrows}

% Ensure single-spaced lines
\usepackage{setspace}
\singlespacing

% Include .pdf files in this one
\usepackage{pdfpages}

% Used for typesetting C++ symbol
\usepackage{relsize}

% Used to remove empty date field in bibliography
% https://tex.stackexchange.com/a/151264
\usepackage{xpatch}
